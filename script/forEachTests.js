"use strict";

// @@use forEach method:
// const array = [1, 2, 3];
// const newArray = [];

// array.forEach((el) => {
//   newArray.push(el * 2);
// });
// console.log(newArray);

// @@use map method:
const array = [1, 2, 3];

const newArray = array.map((el) => el * 2);
console.log(newArray);
